default:
    extensions:
        Laracasts\Behat: ~
            # env_path: .env.behat
        Behat\MinkExtension:
            default_session: laravel
            base_url: http://customermanagement.app:8000
            laravel: ~